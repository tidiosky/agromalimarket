$(document).ready(function () {
    $('#district').click(function () {
        var distval = $('#district').val();
        $.post('http://127.0.0.1:8000/searchCity',{distval:distval},function (math) {
            $('#city').html(math)
        });
    });
});